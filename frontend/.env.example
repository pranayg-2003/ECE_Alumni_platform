# Copy to .env.local for local overrides (optional).
# Production uses .env.production when you run npm run build.

# Full REST API base URL (must end with /api)
REACT_APP_API_URL=http://localhost:5000/api
